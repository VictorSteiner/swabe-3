{
  "name": "nodesender",
  "version": "1.0.0",
  "description": "",
  "main": "sender.ts",
  "scripts": {
    "compile": "tsc sender.ts --outDir build",
    "send": "node build/sender.js",
    "start": "yarn compile && yarn send"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "amqplib": "^0.8.0",
    "dotenv": "^16.0.0",
    "rabbit": "^0.0.5",
    "typescript": "^4.6.3"
  },
  "devDependencies": {
    "@types/amqplib": "^0.8.2"
  }
}
